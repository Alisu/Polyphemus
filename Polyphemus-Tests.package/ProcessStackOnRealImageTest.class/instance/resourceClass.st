configuring
resourceClass

	"The image opened the way a running VM has it: the initial context married to a frame.
	Stage one reads the same file as a snapshot, where there are no frames at all."

	^ Pharo10ImageResource
