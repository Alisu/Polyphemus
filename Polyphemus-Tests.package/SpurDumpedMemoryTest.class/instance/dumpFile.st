running
dumpFile

	"A core of a real VM. It is 200 MB, so it is not in the repository:
	`bin/take-dump.sh` makes one, and these tests skip without it."

	^ FileLocator home / 'polyphemus' / 'pharo.core'
