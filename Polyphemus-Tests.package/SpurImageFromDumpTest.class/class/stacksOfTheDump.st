accessing
stacksOfTheDump

	"Each process of the dump as its name -> how many activations its stack holds, counted
	before it was written: writing turns the fixture's frames into contexts."

	^ Stacks
