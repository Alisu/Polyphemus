running
defaultTimeLimit

	"Launching another image and waiting for it to load its sixty megabytes takes longer than
	SUnit's ten seconds, and a test that trips that watchdog reports something unrelated."

	^ 2 minutes
