What came of walking objects from one address: how many were well formed, where the walk
stopped, and what stopped it.

The count is the whole point. A Pharo heap walks for millions of objects; two megabytes of noise
managed two. It is the length of the run that says a region is a heap, not any single header.
