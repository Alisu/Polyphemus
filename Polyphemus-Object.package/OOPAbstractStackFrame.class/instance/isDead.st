context protocol
isDead

	"A frame on a live page is running, whether or not it is the one the interpreter's
	registers describe. Only the active frame can say where its pc is, and that is not the
	same question."

	^ false
