accessing
oopCallerContext

	"The activation that called this one, when it is not a frame on this page. Only the
	oldest frame of a page has one; every other frame was called by the frame below it."

	^ nil
