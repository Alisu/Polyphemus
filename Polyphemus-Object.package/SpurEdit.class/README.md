Edits aimed at what a reading shows, written into a memory holding that image: an object's slot, or a method's code replaced by code compiled here that fits where it sits (rung 1 of #25). The memory says how to write; this class turns what the reading shows into addresses there.
