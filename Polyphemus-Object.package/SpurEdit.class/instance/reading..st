editing
reading: aReifiedMemory

	"The reading these edits are aimed at, so a slot can be named by the object an inspector
	shows rather than by an address."

	reading := aReifiedMemory.
	shift := self shiftOf: aReifiedMemory
