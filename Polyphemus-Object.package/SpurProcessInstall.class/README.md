Putting methods and what they name into an image that is running. The same passes as an image file's, with three things different: it allocates where the machine does, in eden, because old space in a running image ends at its bridge; it tells the collector when an old object comes to point at a young one; and it writes through /proc/<pid>/mem, which refuses unless the process is stopped.
