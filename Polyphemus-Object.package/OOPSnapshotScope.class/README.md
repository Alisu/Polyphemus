The scope of a frame read out of a snapshot: empty, for now.

Naming temporaries needs the method syntax tree analysed against the classes of the
image it came from. Until stage one does that, a frame offers its receiver and its
stack, and no temporaries, rather than pretending.
