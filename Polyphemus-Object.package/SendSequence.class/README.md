The sends a piece of compiled code makes, in order, with the pc of each -- decoded by Pharo's own InstructionStream, so it reads the file's code (a reified method) and ours alike.

Two codes making the same sends in the same order correspond send for send even when their bytes differ, which is how a pc of the file's code is placed in code compiled here by a different compiler (#4).
